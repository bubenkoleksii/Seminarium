'use client';

import { FC } from 'react';

type CreateParentSchoolProfileProps = {
  type: string;
  invitationCode: string;
};

const CreateParentSchoolProfile: FC<CreateParentSchoolProfileProps> = ({
  type,
  invitationCode,
}) => {
  return <div></div>;
};

export { CreateParentSchoolProfile };
